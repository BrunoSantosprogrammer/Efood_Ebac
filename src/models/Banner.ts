class Banners {
  title: string
  category: string
  Image: string
  id: number

  constructor(title: string, category: string, Image: string, id: number) {
    this.title = title
    this.category = category
    this.Image = Image
    this.id = id
  }
}

export default Banners
