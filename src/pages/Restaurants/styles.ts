import styled from 'styled-components'
import { HeaderBar } from '../../components/Header/style'

export const ContainerProducts = styled.div`
  ${HeaderBar} {
    height: 186px;
    margin-bottom: 0;
  }
`
